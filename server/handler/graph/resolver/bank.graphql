type Bank{
    id: ID!
    name: String!
}

extend type Query {
    banks: [Bank!]!
}

extend type Mutation {
    createBank(input: createBankInput!): Bank!
}

input createBankInput {
    name: String!
}
